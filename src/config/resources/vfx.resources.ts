import type { ResourceGroup } from "@engine-types/resources";

export const vfxResources: ResourceGroup = {
  items: [
    // { id: "particles", url: "/vfx/particles.json" },
  ],
  loader: "vfx",
};
