import type { ResourceGroup } from "@engine-types/resources";

export const textureResources: ResourceGroup = {
  items: [
    // { id: "ground", url: "/textures/ground.jpg" },
  ],
  loader: "texture",
};
